export type HeaderType = {
  backButton?: boolean;
  isLogo?: boolean;
  burgerMenuVisibility?: boolean;
  pageTitle?: string;
  notification?: boolean;
  customBack?: () => void;
  activeNotifications?: boolean;
};
